name: Run at 9:35 AM Ukraine Time

on:
  schedule:
    - cron: '15 7 * * *'

jobs:
  run-task:
    name: Run every at time
    runs-on: ubuntu-latest
    steps:
      - run: |
          date -u
          echo "This job runs at 09:27 Ukraine time (06:35 UTC)"